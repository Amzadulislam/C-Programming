#include<stdio.h>
#include <math.h>
#include<stdbool.h>

int main()
{
    /*  Relational Operators
        == equals to
        < less than
        > greater than
        <= less or equal
        >= greater or equal
        != not equal
        ALL Tag = flag
        ture = 1 
        false = 0
    */
   int a = 9, b =  10 ;
   // bool isEqual,less than,not Equal,ext = flag  
   bool flag = a <= b ;
   printf("%d", flag ) ;
    
    return 0;
}
